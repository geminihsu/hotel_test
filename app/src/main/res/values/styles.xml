<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="CustomTheme" parent="android:Theme.Holo.Light">

        <item name="android:textSize">@dimen/default_text_size_sp</item>
        <item name="android:textColor">#000000</item>
        <!-- 變更actionbar 背景 -->
        <!-- <item name="actionBarStyle">@style/Widget.MyTheme.ActionBar</item> -->
        <item name="android:actionBarStyle">@style/Widget.MyTheme.ActionBar</item>
        <!-- actionbar item 顏色 -->
        <item name="android:actionMenuTextColor">@android:color/white</item>
        <item name="android:actionMenuTextAppearance">@style/MyActionBar.MenuTextStyle</item>
    </style>
    <style name="Widget.MyTheme.ActionBar" parent="android:Widget.Holo.Light.ActionBar">
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <item name="android:background">@drawable/bg_header_title_view</item>
        <item name="android:textColor">@color/white</item>
        <!-- <item name="background">#96A6BD</item> -->
    </style>
    <style name="MyActionBar.MenuTextStyle" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Menu">
        <item name="android:textColor">@color/white</item>
        <item name="android:textAllCaps">false</item>
    </style>
    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText" parent="@android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@color/colorPrimaryDark</item>
        <!-- The textColor property is backward compatible with the Support Library -->
    </style>
</resources>
